#
# Cntlm Authentication Proxy Configuration
#

Username  {{ username }}
Domain    {{ domain }}
Password  {{ password}}

# NOTE: Only use plaintext password at your own risk
#       Hashes should be used whereaver possible instead.
#       You can use a "cntlm -M" and "cntlm -H" command
#       sequence to get the right config for your environment.
#       See cntlm man page
#PassLM          {{ pass_lm }}
#PassNT          {{ pass_nt }}
#PassNTLMv2      {{ pass_ntlm }}

# List of parent proxies to use. More proxies can be defined
# one per line in format <proxy_ip>:<proxy_port>
Proxy   {{ proxy_address }}

# List addresses you do not want to pass to parent proxies
# * and ? wildcards can be used
# NoProxy   localhost, 127.0.0.*, 10.*, 192.168.*
NoProxy {{ no_proxy_off }}

# Specify the port cntlm will listen on
# You can bind cntlm to specific interface by specifying
# the appropriate IP address also in format <local_ip>:<local_port>
# Cntlm listens on 127.0.0.1:3128 by default
#
Listen {{ listen_ip }}:{{ listen_port }}

